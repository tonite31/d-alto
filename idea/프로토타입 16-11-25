1. 접속하면 맵 위에 캐릭터가 배치되어 있음.
 - 이동할 수 있고 게임 화면 처럼 스크롤링도.
 
2. 서버, 클라 구현부분 모듈화.
3. 클라 -> 프론트 서버 -> 월드 서버 구조로 변경.
4. 맵 데이터 구조 변경
 - 전부 메모리로 들고 있으면 안되고 맵에 배치된 오브젝트들만 가지고 있는다.
 - 변경될 필요 없는 애들과 변경 가능성이 있는 애들로 나눈다.
