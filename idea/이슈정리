키를 계속 누르고 있을때 딜레이생기는것 : key를 눌렀을때 state체크를 해두고 프레임마다 setInterval을 날리고 keyup할때 해제하라.
오브젝트를 움직이는건 transform translate3D를 이용. 그리고 transition: transform 0.05s linear; 을 걸어라.

클라이언트가 요청하면 서버에서 맵데이터를 보냈다. 항상 보내지 않는다.

충돌체크는 존을 나눈다.

화면 드로우는 현재 스크린에 보이는 존의 오브젝트만 한다.

맵 스크롤 속도가 캐릭터 이동속도보다 빨라야 스무스하다.